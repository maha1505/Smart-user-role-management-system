{
  "name": "rolecore-unified",
  "version": "1.0.0",
  "description": "Unified build script for RoleCore MERN App",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client --include=dev",
    "build-client": "npm install --prefix client --include=dev && npm run build --prefix client",
    "start": "npm start --prefix server"
  }
}
